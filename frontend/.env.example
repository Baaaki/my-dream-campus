# API Base URL. Empty = same-origin (dev: Vite proxy /api -> monolith:8080,
# prod: Caddy serves SPA + /api on one origin). Set only for a remote API.
VITE_API_BASE_URL=

# Use Mock API (set to 'true' to use mock data, 'false' for real API)
VITE_USE_MOCK_API=true
