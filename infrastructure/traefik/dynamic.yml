# Traefik Dynamic Configuration
#
# This file contains routing rules for microservices
# Add your service routes here as you develop them
#
# Example:
# http:
#   routers:
#     auth-service:
#       rule: "PathPrefix(`/api/auth`)"
#       service: auth-service
#       middlewares:
#         - strip-prefix
#
#   services:
#     auth-service:
#       loadBalancer:
#         servers:
#           - url: "http://auth-service:8001"
#
#   middlewares:
#     strip-prefix:
#       stripPrefix:
#         prefixes:
#           - "/api/auth"

http:
  routers: {}
  services: {}
  middlewares: {}
